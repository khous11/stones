(function(board) {
  board.views = {};
  var v = board.views;
  
  v.BoardView = Backbone.View.extend({
    
    initialize: function(args) {
    },
    
    
    render: function() {
    },
    
  });
  
})(window.board = window.board || {});
